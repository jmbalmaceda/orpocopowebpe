<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:task="http://www.springframework.org/schema/task"
	xmlns:context="http://www.springframework.org/schema/context" xmlns:tx="http://www.springframework.org/schema/tx"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="
http://www.springframework.org/schema/beans
http://www.springframework.org/schema/beans/spring-beans-4.1.xsd
http://www.springframework.org/schema/context
http://www.springframework.org/schema/context/spring-context-4.1.xsd
http://www.springframework.org/schema/tx
http://www.springframework.org/schema/tx/spring-tx-4.1.xsd
http://www.springframework.org/schema/task http://www.springframework.org/schema/task/spring-task-4.1.xsd">
			
	<tx:annotation-driven/>
	
	
	<!-- Configuración de base de datos usando JDBC -->
	<bean id="orcoDataSource" class="com.mchange.v2.c3p0.ComboPooledDataSource" destroy-method="close" lazy-init="true">
		<property name="driverClass" value="${reportCenter.hibernate.connection.driver_class}"/>
		<property name="jdbcUrl" value="${reportCenter.hibernate.connection.url}"/>

		<property name="user" value="${reportCenter.hibernate.connection.username}"/>
		<property name="password" value="${reportCenter.hibernate.connection.password}"/>
		
		<property name="minPoolSize" value="${hibernate.c3p0.min_size}"/>
		<property name="maxPoolSize" value="${hibernate.c3p0.max_size}"/>
		<property name="maxStatements" value="${hibernate.c3p0.c3p0.max_statements}"/> 
		<property name="preferredTestQuery" value="${hibernate.connection.validationQuery}"/>
	</bean>
	
	
	<!-- Session Factory -->
	<bean id="orcoSessionFactory" class="org.springframework.orm.hibernate4.LocalSessionFactoryBean">
        <property name="dataSource" ref="orcoDataSource"></property>
        <property name="configLocation" value="classpath:hibernate-orco.cfg.xml" />
        <property name="hibernateProperties">
            <props>
                <prop key="hibernate.dialect">${reportCenter.hibernate.dialect}</prop>
                <prop key="hibernate.default_schema">${reportCenter.hibernate.default_schema}</prop>
            </props>
        </property>
    </bean>

	<bean id="orcoTransactionManager"  class="org.springframework.orm.hibernate4.HibernateTransactionManager">
        <property name="sessionFactory" ref="orcoSessionFactory" />
        <qualifier value="orcoTransactionManager"></qualifier>
    </bean>
   
	<!-- END: Beans del módulo reportCenter -->
</beans>